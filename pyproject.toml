[project]
name = "medest-open-starter"
version = "0.1.0"
description = "Starter implementation for MedEST-Open and EpiDelta-Lite"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["medest*"]
